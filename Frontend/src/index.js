import React from "react";
import ReactDOM from "react-dom";

import App from "./App"; //grab the app js file.

ReactDOM.render(<App />, document.getElementById("root"));
//so here is where we are rendering the site, we first grab the appjs file and then use the id root from
//the index.html file form from the public folder and insert the data into it.
